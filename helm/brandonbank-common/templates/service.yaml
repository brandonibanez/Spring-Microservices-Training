apiVersion: v1
kind: Service
metadata:
  name: {{ include "brandonbank-common.fullname" . }}
  labels:
    {{- include "brandonbank-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "brandonbank-common.selectorLabels" . | nindent 4 }}
