apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brandonbank-common.fullname" . }}-test-connection"
  labels:
    {{- include "brandonbank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brandonbank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
